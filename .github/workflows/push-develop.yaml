name: Pushed to develop

on:
  push:
    branches:
      - develop

jobs:
  version-bump:
    permissions:
      contents: write
    uses: ./.github/workflows/template/version-bump.yaml

  deploy:
    needs: version-bump
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deploying to develop"
